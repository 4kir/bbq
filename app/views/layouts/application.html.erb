<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Шашлыки" %></title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag "application", :media => "all" %>

  <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

  <%= javascript_pack_tag "application" %>

</head>
<body>

  <nav id="navbar-example2" class="navbar navbar-light bg-light px-3 border-bottom border-2 buffer-bottom">
    <div class="container">
      <%= link_to t('app.name'), root_path, class: 'navbar-brand' %>
      <ul class="nav nav-pills">
        <% if user_signed_in? %>
          <li><%= link_to t('viewes.profile'), current_user, class: 'nav-link' %></li>
          <li><%= link_to t('viewes.log_out'), destroy_user_session_path, :method => :delete, class: 'nav-link' %></li>
        <% else %>
          <li><%= link_to t('viewes.sign_in'), new_user_session_path, class: 'nav-link' %></li>
          <li><%= link_to t('viewes.registration'), new_user_registration_path, class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </nav>
      <%# Для вывода названия приложения используем хелпер I18n.t %>

  <div class="container">
    <div class="row">
      <div class="col-lg-9">
        <%= bootstrap_flash %>

        <%= yield %>
      </div>
      <div class="col-lg-3">
      </div>
    </div>

    <footer>
      <%# Для вывода названия компании используем хелпер I18n.t %>
      <p class="buffer-top">&copy; <%= t 'app.company' %> 2020</p>
    </footer>
  </div>

</body>
</html>
