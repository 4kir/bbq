<h1><%= t('viewes.profile_editing') %></h1>

<div class="row">
  <div class="col-md-6 buffer-top">
    <%= render 'form' %>

    <p class="buffer-top">
      <%# Ссылка на смену пароля текущего залогиненного юзера (этот путь %>
      <%# генерит девайз) %>
      <%= link_to t('viewes.password_change'), edit_user_registration_path %>
    </p>
  </div>
</div>
